# Tunnel Server Configuration

# Server Port
TUNNEL_SERVER_PORT=9000

# Base domain for tunnels (e.g., myapi.tunnel.arm.dev)
TUNNEL_BASE_URL=tunnel.arm.dev

# Backend API URL
BACKEND_URL=http://localhost:5000

# Internal authentication token (for server-to-server communication)
INTERNAL_AUTH_TOKEN=your_internal_token_here

# SSL/TLS Configuration (for ssl-proxy.js)
NODE_ENV=development
DOMAIN=tunnel.arm.dev
LETSENCRYPT_EMAIL=admin@arm.dev
LETSENCRYPT_STAGING=true

# For production, set:
# NODE_ENV=production
# LETSENCRYPT_STAGING=false
